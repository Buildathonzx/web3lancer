print(default_api.natural_language_write_file(language = "tsx", path = "src/components/Buttons.tsx", prompt = "In tsx, Create a Buttons component file. This file should contain reusable button components for different styles and functionalities. Include variations for primary, secondary, and tertiary buttons with different colors, sizes, and states (e.g., hover, active, disabled). Style these buttons using the global styles or a separate stylesheet."))